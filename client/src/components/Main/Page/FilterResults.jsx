/*
import React, { Component } from 'react';

class FilterResults extends Component {
  render() {
    let a = ''
    if(this.props.samples.files){
      a = Object.values(this.props.samples.files);
      console.log(a);
      return (
        <div>
          <h1>{this.props.samples.username}</h1>
          {a.map((item, i) => (
          <p key={i}>{item}</p>
            ))}
        </div>
      );
    } else {
      return (
        <p>return latest.</p>
      );
    }
  }
}

export default FilterResults
*/
